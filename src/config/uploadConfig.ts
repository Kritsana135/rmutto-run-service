export const rootServerPath = process.cwd();
export const rootImagesPath = `${rootServerPath}/images`;

export const imageMIMEType = ["image/ief", "image/jpeg", "image/png"];

export const storeDirectory = {
  profile: `${rootImagesPath}/profile`,
  progress: `${rootImagesPath}/progress`,
};
